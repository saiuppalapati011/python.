def factorial(n):
    fact = 1
    if n == 0 or n == 1:
        return 1
    else:
        for i in range(2, n + 1):
            fact *= i 
    return fact
num=int(input("enter the number : "))
print(f"the factorial of {num}  is  {factorial(num)}")
